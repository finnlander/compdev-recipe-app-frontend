import { RequireEqualityValidatorDirective } from './require-equality-validator.directive';

describe('RequireEqualityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireEqualityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
